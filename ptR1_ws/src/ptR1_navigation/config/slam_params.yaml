# พารามิเตอร์สำหรับ slam_toolbox node

slam_toolbox:
  # Frame IDs
  odom_frame: odom
  map_frame: map
  base_frame: base_link

  # Topics
  scan_topic: /scan # ค่านี้จะถูก override โดย 'remap' ใน launch file

  # การตั้งค่าอื่นๆ (นำมาจากโค้ดเดิม)
  use_scan_matching: true
  max_laser_range: 10.0